traffic-gui-indicator

simple gui traffic indicator
shows on small window the traffic spent per session

License:GPLv3+

ChangeLog:
1.0.1
 added --interface
 handles all the command-line arguments, not only the first

1.1.0
 Fixed to be compatible with the new ifconfig
 The command line arguments are processed through gnuopt.h
 When you close the window, the application hides in the tray
 Not resizeble

